project MergeSort is

   package Check is
      for Switches ("main.adb") use ("-d");
   end Check;

   package Builder is
      for Switches ("main.adb") use ("-g");
   end Builder;

   package Compiler is
      for Switches ("main.adb") use ("-g");
   end Compiler;

   package Linker is
      for Switches ("main.adb") use ("-g");
   end Linker;

   for Main use ("main.adb");

end MergeSort;

